{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "pip install -r requirements-prod.txt && python manage.py migrate && gunicorn news_project.wsgi --log-file -"
  }
}